<div id="wrapper">
        <div class="row">
            <div class="col-md-6 col-md-offset-3 box-login">
                <div class="modal-content">
                    <div class="panel-heading">
                      <span><center><i class="fa fa-users fa-5x"></i></center></span>
                      </br>
                        <h1 class="page"><center>Cambia tu contraseña</center></h1>
                    </div>
<div class="panel-body">
  <fieldset>
<%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.error_notification %>

  <%= f.input :reset_password_token, as: :hidden %>
  <%= f.full_error :reset_password_token %>

  <div class="form-inputs">
    <%= f.input :password,
                label: "New password",
                required: true,
                autofocus: true,
                hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :password_confirmation, label: "Confirm your new password", required: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Cambiar mi contraseña", class:"btn btn-success active" %>
  </div>
<% end %>
</br>
<%= render "devise/shared/links" %>
</fieldset>
</div>
</div>
</div>
</div>
</div>
